'use client';
import {useEffect,useMemo,useRef,useState} from 'react';
import {KaraokeLine,activeKaraokeLine,buildEstimatedKaraokeTimeline,exportAss,exportEnhancedLrc,exportSrt,parseKaraokeJson,alignRoughWordsToLyrics,setLineTiming,setWordTiming,shiftTimeline,timelineFromLineStarts} from '../lib/karaoke';
type Props={audioUrl:string;lyrics:string;duration:number;timeline:KaraokeLine[];onChange:(v:KaraokeLine[])=>void};const sec=(v:number)=>Number.isFinite(v)?v.toFixed(2):'0.00';const clamp=(v:number,a:number,b:number)=>Math.max(a,Math.min(b,v));
function saveText(v:string,n:string,t='text/plain;charset=utf-8'){const b=new Blob([v],{type:t}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=n;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}
function Waveform(p:{audioUrl:string;duration:number;time:number;timeline:KaraokeLine[];selected:number;onSeek:(v:number)=>void;onSelect:(i:number)=>void;onChange:(v:KaraokeLine[])=>void}){const{audioUrl,duration,time,timeline,selected,onSeek,onSelect,onChange}=p,ref=useRef<HTMLCanvasElement|null>(null),drag=useRef<{i:number;e:'start'|'end'}|null>(null),[peaks,setPeaks]=useState<number[]>([]);useEffect(()=>{let dead=false;(async()=>{try{const r=await fetch(audioUrl,{cache:'no-store'}),C=window.AudioContext||(window as any).webkitAudioContext;if(!C)return;const c=new C(),b=await c.decodeAudioData((await r.arrayBuffer()).slice(0)),d=b.getChannelData(0),bins=400,step=Math.max(1,Math.floor(d.length/bins)),a:number[]=[];for(let i=0;i<bins;i++){let m=0;for(let j=i*step;j<Math.min(d.length,(i+1)*step);j++)m=Math.max(m,Math.abs(d[j]));a.push(m)}await c.close();if(!dead)setPeaks(a)}catch{}})();return()=>{dead=true}},[audioUrl]);useEffect(()=>{const c=ref.current;if(!c)return,w=Math.max(300,c.clientWidth),h=140,dpr=Math.min(2,window.devicePixelRatio||1);c.width=w*dpr;c.height=h*dpr;const x=c.getContext('2d');if(!x)return;x.setTransform(dpr,0,0,dpr,0,0);x.fillStyle='#09111e';x.fillRect(0,0,w,h);x.strokeStyle='rgba(148,163,184,.35)';const mid=h/2,bw=w/Math.max(1,peaks.length);peaks.forEach((q,i)=>{const ph=Math.max(1,q*h*.43);x.beginPath();x.moveTo(i*bw,mid-ph);x.lineTo(i*bw,mid+ph);x.stroke()});timeline.forEach((l,i)=>{const a=l.start/Math.max(.01,duration)*w,b=l.end/Math.max(.01,duration)*w;x.fillStyle=i===selected?'rgba(217,70,239,.22)':'rgba(99,102,241,.12)';x.fillRect(a,0,Math.max(2,b-a),h);x.strokeStyle=i===selected?'#f0abfc':'#818cf8';x.strokeRect(a+.5,1,Math.max(1,b-a-1),h-2)});x.fillStyle='#22d3ee';x.fillRect(time/Math.max(.01,duration)*w-1,0,2,h)},[peaks,duration,time,timeline,selected]);const position=(e:any)=>{const r=e.currentTarget.getBoundingClientRect();return clamp((e.clientX-r.left)/r.width,0,1)*duration};const down=(e:any)=>{const t=position(e),th=Math.max(.12,duration*.012);let hit=-1,edge:'start'|'end'='start',best=Infinity;timeline.forEach((l,i)=>{const ds=Math.abs(t-l.start),de=Math.abs(t-l.end);if(ds<best&&ds<th){best=ds;hit=i;edge='start'}if(de<best&&de<th){best=de;hit=i;edge='end'}});if(hit>=0){drag.current={i:hit,e:edge};onSelect(hit);e.currentTarget.setPointerCapture(e.pointerId)}else{const i=timeline.findIndex(l=>t>=l.start&&t<=l.end);if(i>=0)onSelect(i);onSeek(t)}};const move=(e:any)=>{if(!drag.current)return;const t=position(e),d=drag.current,l=timeline[d.i];if(!l)return;onChange(setLineTiming(timeline,d.i,d.e==='start'?Math.min(t,l.end-.02):l.start,d.e==='end'?Math.max(t,l.start+.02):l.end))};return <div className="mt-4"><div className="mb-2 flex justify-between gap-2"><b className="text-xs text-white/70">Waveform Lyrics Editor</b><span className="text-[10px] text-white/35">Kéo mép vùng lời để chỉnh timing</span></div><canvas ref={ref} onPointerDown={down} onPointerMove={move} onPointerUp={()=>drag.current=null} onPointerCancel={()=>drag.current=null} className="h-[140px] w-full touch-none rounded-xl border border-white/10"/></div>}
export default function KaraokeEditor({audioUrl,lyrics,duration,timeline,onChange}:Props){const audio=useRef<HTMLAudioElement|null>(null),input=useRef<HTMLInputElement|null>(null),[time,setTime]=useState(0),[selected,setSelected]=useState(0),[tap,setTap]=useState<number[]>([]),[tapMode,setTapMode]=useState(false),[words,setWords]=useState<number|null>(null),[aligning,setAligning]=useState(false),[msg,setMsg]=useState(''),active=useMemo(()=>activeKaraokeLine(timeline,time),[timeline,time]);useEffect(()=>{if(active.index>=0)setSelected(active.index)},[active.index]);const seek=(v:number)=>{if(audio.current){audio.current.currentTime=clamp(v,0,duration);setTime(audio.current.currentTime)}};async function align(){setAligning(true);setMsg('Đang Auto Sync...');try{const r=await fetch(audioUrl,{cache:'no-store'}),C=window.AudioContext||(window as any).webkitAudioContext;if(!C)throw Error('Web Audio không hỗ trợ');const c=new C(),d=await c.decodeAudioData((await r.arrayBuffer()).slice(0)),off=new OfflineAudioContext(1,Math.ceil(d.duration*16000),16000),s=off.createBufferSource();s.buffer=d;s.connect(off.destination);s.start(0);const pcm=new Float32Array((await off.startRendering()).getChannelData(0));await c.close();const w=new Worker(new URL('../workers/karaoke-whisper.worker.ts',import.meta.url),{type:'module'}),res:any=await new Promise((ok,no)=>{w.onmessage=e=>e.data?.type==='result'?ok(e.data):e.data?.type==='error'&&no(Error(e.data.message));w.onerror=()=>no(Error('Whisper worker lỗi'));w.postMessage({type:'transcribe',audio:pcm.buffer,language:'vi'},[pcm.buffer])}).finally(()=>w.terminate()),rough=res.chunks.map((q:any)=>({text:q.text,start:Number(q.timestamp?.[0]),end:Number(q.timestamp?.[1])})).filter((q:any)=>Number.isFinite(q.start)&&Number.isFinite(q.end)),v=alignRoughWordsToLyrics(lyrics,rough,duration);if(!v.length)throw Error('Không căn được lyrics');onChange(v);setMsg('Auto Sync hoàn tất.')}catch(e){setMsg(e instanceof Error?e.message:'Auto Sync lỗi')}finally{setAligning(false)}}async function imp(f?:File){if(f)onChange(parseKaraokeJson(await f.text()));if(input.current)input.current.value=''}return <div className="mt-4 rounded-2xl border border-white/10 bg-black/25 p-4"><div className="flex flex-wrap justify-between gap-2"><div><b>Karaoke timing editor</b><p className="text-xs text-white/45">Waveform + Tap Sync + chỉnh timing.</p></div><div className="flex flex-wrap gap-2"><button onClick={()=>onChange(buildEstimatedKaraokeTimeline(lyrics,duration))} className="rounded border px-2 text-xs">Estimate</button><button onClick={()=>void align()} disabled={aligning} className="rounded bg-fuchsia-300/10 px-2 text-xs">{aligning?'Sync...':'Auto Sync'}</button><button onClick={()=>{if(audio.current){audio.current.currentTime=0;setTap([]);setTapMode(true);void audio.current.play()}}} className="rounded bg-amber-300/10 px-2 text-xs">Tap Sync</button>{tapMode&&<button onClick={()=>{if(!audio.current)return;const n=[...tap,audio.current.currentTime];setTap(n);if(n.length>=timeline.length){setTapMode(false);onChange(timelineFromLineStarts(lyrics,n,duration))}}} className="rounded bg-amber-300 px-2 text-xs text-black">Tap câu</button>}</div></div><audio ref={audio} src={audioUrl} controls onTimeUpdate={e=>setTime(e.currentTarget.currentTime)} className="mt-4 h-10 w-full"/>{msg&&<p className="mt-2 text-xs text-white/50">{msg}</p>}<Waveform audioUrl={audioUrl} duration={duration} time={time} timeline={timeline} selected={selected} onSeek={seek} onSelect={setSelected} onChange={onChange}/><div className="mt-3 flex gap-2"><input className="flex-1" type="range" min={0} max={duration||1} step=.01 value={time} onChange={e=>seek(+e.target.value)}/><span className="text-xs">{sec(time)}/{sec(duration)}</span></div><div className="mt-3 flex flex-wrap gap-2"><button onClick={()=>onChange(shiftTimeline(timeline,-.1))}>-100ms</button><button onClick={()=>onChange(shiftTimeline(timeline,.1))}>+100ms</button><button onClick={()=>saveText(JSON.stringify({lines:timeline},null,2),'karaoke.json')}>JSON</button><button onClick={()=>saveText(exportAss(timeline),'karaoke.ass')}>ASS</button><button onClick={()=>saveText(exportEnhancedLrc(timeline),'karaoke.lrc')}>LRC</button><button onClick={()=>saveText(exportSrt(timeline),'karaoke.srt')}>SRT</button><button onClick={()=>input.current?.click()}>Import</button><input ref={input} type="file" accept=".json" className="hidden" onChange={e=>void imp(e.target.files?.[0])}/></div><div className="mt-4 max-h-96 space-y-2 overflow-auto">{timeline.map((l,i)=><div key={i} className={`rounded border p-2 ${i===selected?'border-fuchsia-300':'border-white/10'}`}><button className="w-full text-left" onClick={()=>{setSelected(i);seek(l.start)}}>{l.text}<span className="float-right text-[10px]">{sec(l.start)}–{sec(l.end)}</span></button>{i===selected&&<div className="mt-2 grid grid-cols-2 gap-2"><input type="number" step=.01 value={l.start} onChange={e=>onChange(setLineTiming(timeline,i,+e.target.value,l.end))}/><input type="number" step=.01 value={l.end} onChange={e=>onChange(setLineTiming(timeline,i,l.start,+e.target.value))}/><button className="col-span-2 text-left text-xs" onClick={()=>setWords(words===i?null:i)}>Timing từng từ</button>{words===i&&l.words.map((w,j)=><div key={j} className="col-span-2 grid grid-cols-[1fr_70px_70px] gap-1"><span>{w.text}</span><input type="number" step=.01 value={w.start} onChange={e=>onChange(setWordTiming(timeline,i,j,+e.target.value,w.end))}/><input type="number" step=.01 value={w.end} onChange={e=>onChange(setWordTiming(timeline,i,j,w.start,+e.target.value))}/></div>)}</div>}</div>)}</div></div>}
