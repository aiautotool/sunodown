self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{}const title=data.title||'Suno Grab';const options={body:data.body||'Video của bạn đã render xong.',icon:'/favicon.ico',badge:'/favicon.ico',tag:data.jobId?`render-${data.jobId}`:'suno-render',data:{url:data.url||(data.jobId?`/?renderJob=${encodeURIComponent(data.jobId)}`:'/')}};event.waitUntil(self.registration.showNotification(title,options))});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=new URL(event.notification.data?.url||'/',self.location.origin).href;event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const client of list){if('focus'in client){client.navigate(url);return client.focus()}}return clients.openWindow(url)}))});
